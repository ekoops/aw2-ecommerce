version: "3.9"
services:
  catalog-svc:
    image: aw2-ecommerce/catalog-svc
    container_name: catalog-svc
    build:
      dockerfile: ../../Dockerfile # relative to the build context
      context: ./catalog-service/build/libs
    env_file:
      - envs/catalog-svc.env.prod
    ports:
      - "8080:8080"
    networks:
      - catalog-net
      - communication-net
    depends_on:
      - catalog-db
      - kafka
      - wallet-svc
  #      - discovery-svc
      - mail-svc
  #      - order-svc
  #    logging:
  #      driver: none

  catalog-db:
    image: mariadb
    container_name: catalog-db
#    restart: always
    env_file:
      - envs/catalog-db.env.prod
    #    ports:
    #      - "3306:3306"
#    volumes:
#      - ./volumes/catalog-db:/var/lib/mysql
    networks:
      - catalog-net
#    logging:
#      driver: none

  wallet-svc:
    image: aw2-ecommerce/wallet-svc
    container_name: wallet-svc
    build:
      dockerfile: ../../Dockerfile # relative to the build context
      context: ./wallet-service/build/libs
    env_file:
      - envs/wallet-svc.env.prod
#    ports:
#      - "4000:4000"
    networks:
      - wallet-net
      - communication-net
    depends_on:
      - wallet-db
      - kafka
  #      - discovery-svc
  #    logging:
  #      driver: none

  wallet-db:
    image: mariadb
    container_name: wallet-db
    restart: always
    env_file:
      - envs/wallet-db.env.prod
    #    ports:
    #      - "3306:3306"
#    volumes:
#      - ./volumes/wallet-db:/var/lib/mysql
    networks:
      - wallet-net
    logging:
      driver: none

  order-svc:
    image: aw2-ecommerce/order-svc
    container_name: order-svc
    build:
      dockerfile: ../Dockerfile # relative to the build context
      context: ./order-service/build
    #    restart: always
    env_file:
      - envs/order-svc.env.prod
    ports:
      - "3000:3000"
    networks:
      - order-net
      - communication-net
    depends_on:
      - order-db
      - kafka
  #      - discovery-svc

  order-db:
    image: mongo
    container_name: order-db
    #    restart: always
    env_file:
      - envs/order-db.env.prod
    #    ports:
    #      - "27017:27017"
    #    volumes:
    #      - ./volumes/order-db:/data/db
    networks:
      - order-net
    logging:
      driver: none

  mail-svc:
    image: aw2-ecommerce/mail-svc
    container_name: mail-svc
    build:
      dockerfile: ../Dockerfile # relative to the build context
      context: ./mail-service/build
    restart: always
    env_file:
      - envs/mail-svc.env.prod
    networks:
      - communication-net
    depends_on:
      - kafka
#      - discovery-svc

  zookeeper:
    image: zookeeper
    container_name: zookeeper
    #    ports:
    #      - "2181:2181"
    volumes:
      - ./volumes/zookeeper/data:/data
      - ./volumes/zookeeper/datalog:/datalog
      - ./volumes/zookeeper/logs:/logs
    networks:
      - zookeeper-kafka-net
    logging:
      driver: none

  kafka:
    image: confluentinc/cp-kafka
    container_name: kafka
    #    ports:
    #      - "9092:9092"
    env_file:
      - ./envs/kafka.env.prod
    volumes:
      #      - ./volumes/kafka/secrets:/etc/kafka/secrets # not needed
      - ./volumes/kafka/data:/var/lib/kafka/data
    networks:
      - zookeeper-kafka-net
      - communication-net
    depends_on:
      - zookeeper
  #    logging:
  #      driver: none

  discovery-svc:
    image: aw2-ecommerce/discovery-svc
    container_name: discovery-svc
    build:
      dockerfile: ../../Dockerfile # relative to the build context
      context: ./discovery-service/build/libs
    ports:
      - "8761:8761"
    networks:
      - communication-net
#    logging:
#      driver: none

networks:
  catalog-net:
    name: catalog-net
  wallet-net:
    name: wallet-net
  order-net:
    name: order-net
  zookeeper-kafka-net:
    name: zookeeper-kafka-net
  communication-net:
    name: communication-net